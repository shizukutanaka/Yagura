diff --git a/yagura-v0.34.1/cmd/yagura/httpapi.go b/yagura-v0.34.1/cmd/yagura/httpapi.go
index 1a6717a..f2877ff 100644
--- a/yagura-v0.34.1/cmd/yagura/httpapi.go
+++ b/yagura-v0.34.1/cmd/yagura/httpapi.go
@@ -14,6 +14,7 @@ package main
 
 import (
 	"context"
+	"crypto/subtle"
 	"encoding/json"
 	"errors"
 	"io"
@@ -80,7 +81,11 @@ func (d *httpAPIDeps) authMiddleware(h http.HandlerFunc) http.HandlerFunc {
 		}
 		auth := r.Header.Get("Authorization")
 		const prefix = "Bearer "
-		if !strings.HasPrefix(auth, prefix) || auth[len(prefix):] != d.AuthToken {
+		// 長さが違う場合も constant-time にするため、HasPrefix を通った場合は
+		// 必ず ConstantTimeCompare まで実行する(/mcp と同じタイミング攻撃対策)。
+		received := strings.TrimPrefix(auth, prefix)
+		if !strings.HasPrefix(auth, prefix) ||
+			subtle.ConstantTimeCompare([]byte(received), []byte(d.AuthToken)) != 1 {
 			http.Error(w, `{"error":"unauthorized"}`, http.StatusUnauthorized)
 			return
 		}
diff --git a/yagura-v0.34.1/internal/aiverify/aiverify.go b/yagura-v0.34.1/internal/aiverify/aiverify.go
index 5b3c333..7491de7 100644
--- a/yagura-v0.34.1/internal/aiverify/aiverify.go
+++ b/yagura-v0.34.1/internal/aiverify/aiverify.go
@@ -33,6 +33,7 @@ import (
 	"regexp"
 	"sort"
 	"strings"
+	"sync"
 )
 
 // RiskLevel は finding の重大度。
@@ -115,8 +116,21 @@ var aiMarkerRe = regexp.MustCompile(`(?i)(?:^|[^a-zA-Z0-9_])(ai[ -]?generated|ge
 // AI が生成した関数は通常 5-30 行。±10 行を保守的に取る。
 const aiZoneRadius = 10
 
+// compiledDefaultRules は正規表現を一度だけコンパイルして再利用する。
+// 以前は DefaultRules() 呼び出し毎に 20+ 個の regexp.MustCompile が走り、
+// scan するファイルごとに同一パターンを再コンパイルしていた。
+var compiledDefaultRules = sync.OnceValue(buildDefaultRules)
+
 // DefaultRules は yagura が出荷するデフォルト rule set。
+// 返り値は呼び出し毎に独立した slice(backing array は共有しない)。
 func DefaultRules() []Rule {
+	rs := compiledDefaultRules()
+	out := make([]Rule, len(rs))
+	copy(out, rs)
+	return out
+}
+
+func buildDefaultRules() []Rule {
 	must := regexp.MustCompile
 	return []Rule{
 		// ─── Auth: CRITICAL ────────────────────────────────────
@@ -368,10 +382,9 @@ func scanImpl(files map[string]string, rules []Rule, cache CacheLike) Result {
 
 		// AI marker 集計(zone とは別途、行数 metric として)
 		lines := strings.Split(content, "\n")
-		_, markers := findAIZones(lines)
+		zones, markers := findAIZones(lines)
 		if markers > 0 {
 			// 各 marker から ±aiZoneRadius 行を AI gen と扱う
-			zones, _ := findAIZones(lines)
 			res.AIGenLines += len(zones)
 		}
 
diff --git a/yagura-v0.34.1/internal/atomicfile/atomicfile.go b/yagura-v0.34.1/internal/atomicfile/atomicfile.go
new file mode 100644
index 0000000..2522f8d
--- /dev/null
+++ b/yagura-v0.34.1/internal/atomicfile/atomicfile.go
@@ -0,0 +1,54 @@
+// Package atomicfile provides crash-safe file writes via the standard
+// temp-file + fsync + rename pattern.
+//
+// 動機:
+//
+//	registry / handoff / secrets / mcp が同じ「temp に書いて rename」処理を
+//	個別に実装しており、durability semantics が揃っていなかった
+//	(secrets と mcp は fsync を欠いていた)。ここに一本化することで
+//	「rename は atomic、かつ data は rename 前に fsync 済み」を全箇所で保証する。
+package atomicfile
+
+import (
+	"os"
+	"path/filepath"
+)
+
+// Write は data を path へ atomic に書き込む。
+//
+// 手順: 同一ディレクトリに temp file を作成 → Write → Chmod(mode) →
+// fsync → Close → Rename(temp, path)。rename は POSIX 上 atomic なので、
+// 読み手は常に「古い完全なファイル」か「新しい完全なファイル」のどちらかを
+// 見る(torn write は起きない)。fsync により rename 前に data が物理媒体へ
+// 到達することを保証する。
+//
+// 親ディレクトリが無ければ作成する。失敗時は temp file を残さない。
+func Write(path string, data []byte, mode os.FileMode) error {
+	dir := filepath.Dir(path)
+	if err := os.MkdirAll(dir, 0o755); err != nil {
+		return err
+	}
+	tmp, err := os.CreateTemp(dir, ".atomic-*")
+	if err != nil {
+		return err
+	}
+	tmpName := tmp.Name()
+	defer os.Remove(tmpName) // error path のクリーンアップ(rename 成功後は no-op)
+
+	if _, err := tmp.Write(data); err != nil {
+		tmp.Close()
+		return err
+	}
+	if err := tmp.Chmod(mode); err != nil {
+		tmp.Close()
+		return err
+	}
+	if err := tmp.Sync(); err != nil {
+		tmp.Close()
+		return err
+	}
+	if err := tmp.Close(); err != nil {
+		return err
+	}
+	return os.Rename(tmpName, path)
+}
diff --git a/yagura-v0.34.1/internal/audit/audit.go b/yagura-v0.34.1/internal/audit/audit.go
index b8e27f3..300bb01 100644
--- a/yagura-v0.34.1/internal/audit/audit.go
+++ b/yagura-v0.34.1/internal/audit/audit.go
@@ -148,14 +148,25 @@ func (l *Logger) Append(r Record) error {
 }
 
 func (l *Logger) rotateLocked(newDate string) error {
+	// openCurrent が成功するまで currentDate を確定しない。
+	// 先に currentDate を書き換えると、open 失敗時(一時的な disk full / 権限)に
+	// 以降の Append が today==currentDate と誤判定して再 rotate せず、閉じた
+	// 旧ハンドルへ書き続けてプロセス再起動まで恒久的に失敗してしまう。
+	prevDate := l.currentDate
 	if l.file != nil {
 		_ = l.file.Sync()
 		_ = l.file.Close()
+		l.file = nil
 	}
 	l.currentDate = newDate
 	l.seq = 0
 	l.lastHash = ""
-	return l.openCurrent()
+	if err := l.openCurrent(); err != nil {
+		// rollback: 次回 Append で再 rotate を試みられるよう旧日付に戻す。
+		l.currentDate = prevDate
+		return err
+	}
+	return nil
 }
 
 // Close は file を flush + close する。多重呼出は安全。
diff --git a/yagura-v0.34.1/internal/audit/example_test.go b/yagura-v0.34.1/internal/audit/example_test.go
index de67407..9581956 100644
--- a/yagura-v0.34.1/internal/audit/example_test.go
+++ b/yagura-v0.34.1/internal/audit/example_test.go
@@ -2,6 +2,8 @@ package audit_test
 
 import (
 	"fmt"
+
+	"github.com/shizukutanaka/yagura/internal/audit"
 )
 
 // ExampleVerify はディレクトリ内の全 audit log ファイルの hash chain を検証する。
@@ -14,6 +16,8 @@ import (
 //	    fmt.Printf("%s: ok=%v records=%d\n", r.File, r.OK, r.TotalRecords)
 //	}
 func ExampleVerify() {
+	// 実利用は yagura verify subcommand を参照(audit.Verify を呼ぶ)。
+	_ = audit.Verify
 	fmt.Println("see yagura verify subcommand")
 	// Output: see yagura verify subcommand
 }
diff --git a/yagura-v0.34.1/internal/handoff/handoff.go b/yagura-v0.34.1/internal/handoff/handoff.go
index 21165c2..569323c 100644
--- a/yagura-v0.34.1/internal/handoff/handoff.go
+++ b/yagura-v0.34.1/internal/handoff/handoff.go
@@ -29,6 +29,8 @@ import (
 	"path/filepath"
 	"sync"
 	"time"
+
+	"github.com/shizukutanaka/yagura/internal/atomicfile"
 )
 
 // Context は handoff session の全データ。
@@ -107,28 +109,8 @@ func (s *Store) Save(ctx *Context) error {
 	}
 
 	// atomic write: temp → fsync → rename
-	tmp := s.path + ".tmp"
-	f, err := os.OpenFile(tmp, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600)
-	if err != nil {
-		return fmt.Errorf("open tmp: %w", err)
-	}
-	if _, err := f.Write(data); err != nil {
-		_ = f.Close()
-		_ = os.Remove(tmp)
-		return fmt.Errorf("write tmp: %w", err)
-	}
-	if err := f.Sync(); err != nil {
-		_ = f.Close()
-		_ = os.Remove(tmp)
-		return fmt.Errorf("fsync tmp: %w", err)
-	}
-	if err := f.Close(); err != nil {
-		_ = os.Remove(tmp)
-		return fmt.Errorf("close tmp: %w", err)
-	}
-	if err := os.Rename(tmp, s.path); err != nil {
-		_ = os.Remove(tmp)
-		return fmt.Errorf("rename: %w", err)
+	if err := atomicfile.Write(s.path, data, 0o600); err != nil {
+		return fmt.Errorf("save handoff: %w", err)
 	}
 	return nil
 }
diff --git a/yagura-v0.34.1/internal/mcp/tools.go b/yagura-v0.34.1/internal/mcp/tools.go
index 3365f0e..939a67e 100644
--- a/yagura-v0.34.1/internal/mcp/tools.go
+++ b/yagura-v0.34.1/internal/mcp/tools.go
@@ -19,6 +19,7 @@ import (
 	"github.com/shizukutanaka/yagura/internal/aiverify"
 	"github.com/shizukutanaka/yagura/internal/agentmd"
 	"github.com/shizukutanaka/yagura/internal/alertfix"
+	"github.com/shizukutanaka/yagura/internal/atomicfile"
 	"github.com/shizukutanaka/yagura/internal/featurelist"
 	"github.com/shizukutanaka/yagura/internal/initps1"
 	"github.com/shizukutanaka/yagura/internal/initsh"
@@ -412,7 +413,10 @@ func buildSbomTool(d Deps) *Tool {
 				SummaryOnly bool `json:"summary_only"`
 			}
 			if len(args) > 0 {
-				_ = json.Unmarshal(args, &in)
+				if err := json.Unmarshal(args, &in); err != nil {
+					return nil, &ToolError{Code: "invalid_input",
+						Message: "invalid arguments", Cause: err}
+				}
 			}
 			bom, err := d.Sbom.Generate(d.MainModulePath, d.MainVersion)
 			if err != nil {
@@ -978,7 +982,12 @@ func buildReleaseRadarTool(d Deps, cache plantracker.CacheLike) *Tool {
 				Limit    int  `json:"limit"`
 				ScanCode bool `json:"scan_code"` // v0.25: true なら aiverify scan
 			}
-			_ = json.Unmarshal(args, &in)
+			if len(args) > 0 {
+				if err := json.Unmarshal(args, &in); err != nil {
+					return nil, &ToolError{Code: "invalid_input",
+						Message: "invalid arguments", Cause: err}
+				}
+			}
 			if in.Limit <= 0 {
 				in.Limit = 10
 			}
@@ -2127,32 +2136,12 @@ func buildInitShTool(d Deps) *Tool {
 	}
 }
 
-// atomicWriteFile writes via tmp + rename so partial writes never leave a
-// torn file on disk. Permissions are explicit so callers can request 0o755
-// (executable) for init.sh.
+// atomicWriteFile writes via tmp + fsync + rename so partial writes never
+// leave a torn file on disk. Permissions are explicit so callers can request
+// 0o755 (executable) for init.sh.
 //
 // Idempotent — re-running with same content is a no-op visible to callers
 // (rename is atomic on POSIX).
 func atomicWriteFile(path string, data []byte, mode os.FileMode) error {
-	dir := filepath.Dir(path)
-	if err := os.MkdirAll(dir, 0o755); err != nil {
-		return err
-	}
-	tmp, err := os.CreateTemp(dir, ".yagura-write-*")
-	if err != nil {
-		return err
-	}
-	defer os.Remove(tmp.Name()) // cleanup on error path
-	if _, err := tmp.Write(data); err != nil {
-		tmp.Close()
-		return err
-	}
-	if err := tmp.Chmod(mode); err != nil {
-		tmp.Close()
-		return err
-	}
-	if err := tmp.Close(); err != nil {
-		return err
-	}
-	return os.Rename(tmp.Name(), path)
+	return atomicfile.Write(path, data, mode)
 }
diff --git a/yagura-v0.34.1/internal/projectgraph/graph.go b/yagura-v0.34.1/internal/projectgraph/graph.go
index 05ac5f1..a312438 100644
--- a/yagura-v0.34.1/internal/projectgraph/graph.go
+++ b/yagura-v0.34.1/internal/projectgraph/graph.go
@@ -173,26 +173,31 @@ type ImpactResult struct {
 //   - CyclePath: cycle を構成する path(あれば)
 func (g *Graph) Impact(slug string) ImpactResult {
 	r := ImpactResult{Slug: slug}
-	// BFS で全 reverses transitive 探索
+	// BFS で全 reverses transitive 探索。
+	// parent で各ノードへの最短経路(BFS 順)を保持し、cycle 検出時に
+	// slug→…→cur の実経路を復元する。以前は単一の path スライスに全訪問
+	// ノードを蓄積していたため、CyclePath に無関係な兄弟ブランチが混入していた。
 	visited := map[string]bool{}
+	parent := map[string]string{slug: ""}
 	queue := []string{slug}
-	var path []string
 	for len(queue) > 0 {
 		cur := queue[0]
 		queue = queue[1:]
 		for _, n := range g.reverses[cur] {
 			if n == slug {
-				// cycle 検出
+				// cycle 検出。最初に見つかった(=最短)経路のみ採用する。
 				r.HasCycle = true
-				r.CyclePath = append([]string{slug}, append(path, cur, slug)...)
+				if r.CyclePath == nil {
+					r.CyclePath = cyclePathTo(parent, slug, cur)
+				}
 				continue
 			}
 			if visited[n] {
 				continue
 			}
 			visited[n] = true
+			parent[n] = cur
 			queue = append(queue, n)
-			path = append(path, n)
 		}
 	}
 	r.DirectImpact = append([]string(nil), g.reverses[slug]...)
@@ -206,6 +211,21 @@ func (g *Graph) Impact(slug string) ImpactResult {
 	return r
 }
 
+// cyclePathTo は parent map を逆走して slug→…→cur の経路を組み立て、
+// 末尾に slug を付けて循環として閉じた path を返す。
+func cyclePathTo(parent map[string]string, slug, cur string) []string {
+	chain := []string{cur}
+	for x := cur; x != slug; {
+		p := parent[x]
+		if p == "" {
+			break
+		}
+		chain = append([]string{p}, chain...)
+		x = p
+	}
+	return append(chain, slug)
+}
+
 // Dangling は graph drift(depends_on で参照されているが存在しない slug)を返す。
 func (g *Graph) Dangling() []DanglingDep {
 	return append([]DanglingDep(nil), g.dangling...)
diff --git a/yagura-v0.34.1/internal/qualitycheck/qualitycheck.go b/yagura-v0.34.1/internal/qualitycheck/qualitycheck.go
index 3fd9ba1..b9f7ab6 100644
--- a/yagura-v0.34.1/internal/qualitycheck/qualitycheck.go
+++ b/yagura-v0.34.1/internal/qualitycheck/qualitycheck.go
@@ -27,6 +27,7 @@ import (
 	"regexp"
 	"sort"
 	"strings"
+	"sync"
 )
 
 // Severity は finding の重大度。
@@ -94,7 +95,19 @@ func (r *Result) HasProhibited() bool {
 //
 // 偽陽性を避けるため、自動生成ファイル等の除外は呼出側の責任とする
 // (path フィルタは ScanFiles で受け付ける)。
+// compiledDefaultRules は正規表現を一度だけコンパイルして再利用する
+// (DefaultRules() 呼び出し毎の 14 個の regexp.MustCompile を回避)。
+var compiledDefaultRules = sync.OnceValue(buildDefaultRules)
+
+// DefaultRules の返り値は呼び出し毎に独立した slice。
 func DefaultRules() []Rule {
+	rs := compiledDefaultRules()
+	out := make([]Rule, len(rs))
+	copy(out, rs)
+	return out
+}
+
+func buildDefaultRules() []Rule {
 	rules := []Rule{
 		// ─── TypeScript / JavaScript ────────────────────────────
 		{
diff --git a/yagura-v0.34.1/internal/registry/registry.go b/yagura-v0.34.1/internal/registry/registry.go
index 01982c9..0fb853a 100644
--- a/yagura-v0.34.1/internal/registry/registry.go
+++ b/yagura-v0.34.1/internal/registry/registry.go
@@ -19,6 +19,7 @@ import (
 	"sync"
 	"time"
 
+	"github.com/shizukutanaka/yagura/internal/atomicfile"
 	"github.com/shizukutanaka/yagura/internal/project"
 )
 
@@ -191,29 +192,8 @@ func (r *Registry) persist(p *project.Project) error {
 	if err != nil {
 		return fmt.Errorf("marshal: %w", err)
 	}
-	final := r.fileFor(p.Slug)
-	tmp := final + ".tmp"
-	f, err := os.OpenFile(tmp, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600)
-	if err != nil {
-		return fmt.Errorf("create tmp: %w", err)
-	}
-	if _, err := f.Write(data); err != nil {
-		_ = f.Close()
-		_ = os.Remove(tmp)
-		return fmt.Errorf("write tmp: %w", err)
-	}
-	if err := f.Sync(); err != nil {
-		_ = f.Close()
-		_ = os.Remove(tmp)
-		return fmt.Errorf("sync tmp: %w", err)
-	}
-	if err := f.Close(); err != nil {
-		_ = os.Remove(tmp)
-		return fmt.Errorf("close tmp: %w", err)
-	}
-	if err := os.Rename(tmp, final); err != nil {
-		_ = os.Remove(tmp)
-		return fmt.Errorf("rename: %w", err)
+	if err := atomicfile.Write(r.fileFor(p.Slug), data, 0o600); err != nil {
+		return fmt.Errorf("persist %s: %w", p.Slug, err)
 	}
 	return nil
 }
diff --git a/yagura-v0.34.1/internal/secrets/secrets.go b/yagura-v0.34.1/internal/secrets/secrets.go
index 87328dd..7afb5cf 100644
--- a/yagura-v0.34.1/internal/secrets/secrets.go
+++ b/yagura-v0.34.1/internal/secrets/secrets.go
@@ -38,6 +38,8 @@ import (
 	"path/filepath"
 	"strconv"
 	"strings"
+
+	"github.com/shizukutanaka/yagura/internal/atomicfile"
 )
 
 const (
@@ -157,14 +159,8 @@ func (s *Store) Set(name string, plaintext []byte, passphrase string) error {
 	if err != nil {
 		return err
 	}
-	path := s.path(name)
-	tmp := path + ".tmp"
-	if err := os.WriteFile(tmp, encrypted, 0o600); err != nil {
-		return fmt.Errorf("write tmp: %w", err)
-	}
-	if err := os.Rename(tmp, path); err != nil {
-		_ = os.Remove(tmp)
-		return fmt.Errorf("rename: %w", err)
+	if err := atomicfile.Write(s.path(name), encrypted, 0o600); err != nil {
+		return fmt.Errorf("write secret: %w", err)
 	}
 	return nil
 }
diff --git a/yagura-v0.34.1/internal/secretscan/secretscan.go b/yagura-v0.34.1/internal/secretscan/secretscan.go
index 303b565..f870332 100644
--- a/yagura-v0.34.1/internal/secretscan/secretscan.go
+++ b/yagura-v0.34.1/internal/secretscan/secretscan.go
@@ -170,7 +170,19 @@ func ShannonEntropy(s string) float64 {
 // 漏洩カテゴリ、および gitleaks v8 default rules を参考にしている。
 // Go regexp は lookahead/lookbehind 不可のため、gitleaks の TOML rule をそのまま
 // 変換できないものは context-free な regex に簡略化している。
+// compiledDefaultRules は 12 個の正規表現を一度だけコンパイルして再利用する
+// (New() / DefaultRules() 呼び出し毎の MustCompile を回避)。
+var compiledDefaultRules = sync.OnceValue(buildDefaultRules)
+
+// DefaultRules の返り値は呼び出し毎に独立した slice。
 func DefaultRules() []Rule {
+	rs := compiledDefaultRules()
+	out := make([]Rule, len(rs))
+	copy(out, rs)
+	return out
+}
+
+func buildDefaultRules() []Rule {
 	return []Rule{
 		{
 			ID:          "aws-access-key-id",
@@ -251,7 +263,9 @@ func DefaultRules() []Rule {
 			ID:          "database-url-with-password",
 			Description: "Database URL with embedded credentials",
 			Severity:    SeverityHigh,
-			Regex:       regexp.MustCompile(`(?i)\b(postgres(?:ql)?|mysql|mongodb(?:\+srv)?|redis)://[^:\s]+:[^@\s]{4,}@[^/\s]+`),
+			// password は 1 文字以上。scheme が既知の DB なので短いパスワードでも
+			// 資格情報埋め込みは検出する({4,} だと 1-3 文字を見逃していた)。
+			Regex:       regexp.MustCompile(`(?i)\b(postgres(?:ql)?|mysql|mongodb(?:\+srv)?|redis)://[^:\s]+:[^@\s]{1,}@[^/\s]+`),
 		},
 	}
 }
diff --git a/yagura-v0.34.1/internal/telemetry/telemetry.go b/yagura-v0.34.1/internal/telemetry/telemetry.go
index 715fcc2..5e01efc 100644
--- a/yagura-v0.34.1/internal/telemetry/telemetry.go
+++ b/yagura-v0.34.1/internal/telemetry/telemetry.go
@@ -15,6 +15,7 @@ package telemetry
 
 import (
 	"context"
+	"sync"
 	"time"
 )
 
@@ -155,6 +156,7 @@ func (noopSpan) RecordError(_ error)                    {}
 //
 // 本番では NewNoopTracer を使う(メモリリーク防止)。
 type RecordingTracer struct {
+	mu    sync.Mutex
 	spans []*RecordedSpan
 }
 
@@ -187,12 +189,16 @@ func (r *RecordingTracer) StartSpan(ctx context.Context, name string, opts ...Sp
 		StartTime:  cfg.startTime,
 		Attributes: append([]Attribute(nil), cfg.attributes...),
 	}
+	r.mu.Lock()
 	r.spans = append(r.spans, rs)
+	r.mu.Unlock()
 	return ctx, &recordingSpan{rs: rs}
 }
 
 // Spans は記録された全 span を返す(検証用)。
 func (r *RecordingTracer) Spans() []*RecordedSpan {
+	r.mu.Lock()
+	defer r.mu.Unlock()
 	out := make([]*RecordedSpan, len(r.spans))
 	copy(out, r.spans)
 	return out
